import React from "react";
import { CircularProgress } from "@material-ui/core";

const Loading = () => {
	return <CircularProgress className="loading-bar" color="secondary" />;
};

export default Loading;
